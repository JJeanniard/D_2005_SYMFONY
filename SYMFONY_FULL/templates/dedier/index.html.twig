{% extends 'base.html.twig' %}

{% block title %}Dedier index{% endblock %}

{% block body %}
    <h1>Dedier index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for dedier in dediers %}
            <tr>
                <td>{{ dedier.id }}</td>
                <td>{{ dedier.name }}</td>
                <td>{{ dedier.description }}</td>
                <td>
                    <a href="{{ path('dedier_show', {'id': dedier.id}) }}">show</a>
                    <a href="{{ path('dedier_edit', {'id': dedier.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('dedier_new') }}">Create new</a>
{% endblock %}
