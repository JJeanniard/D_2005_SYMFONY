{% extends 'base.html.twig' %}

{% block title %}Dedier{% endblock %}

{% block body %}
    <h1>Dedier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dedier.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dedier.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ dedier.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dedier_index') }}">back to list</a>

    <a href="{{ path('dedier_edit', {'id': dedier.id}) }}">edit</a>

    {{ include('dedier/_delete_form.html.twig') }}
{% endblock %}
